<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        x:Class="MovieNet.MainWindow"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="800"
        DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=MainVm}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Border Grid.Column="0" Grid.ColumnSpan="2" Background="AntiqueWhite" >
        </Border>
        <Border Grid.Row="1" Grid.RowSpan="2" Background="AliceBlue"></Border>
        <Border Grid.Row="1" Grid.Column="1" Background="CadetBlue"></Border>
        <!--<ContentPresenter x:Name="Utilisateurs" Content="{Binding CurrentView}" DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=UserVm}" Grid.ColumnSpan="2" Margin="140,75,10,5" Grid.RowSpan="2"/>-->
        <StackPanel Margin="5" Grid.Column="0" Grid.Row="1">
            <Button Command="{Binding NavigateTo}">Utilisateurs</Button>
        </StackPanel>
    </Grid>
</Window>
